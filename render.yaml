services:
  - type: web
    name: grow-a-garden-stock-tracker
    env: node
    buildCommand: npm install && npx puppeteer browsers install chrome
    startCommand: export PATH="$(npx puppeteer browsers resolve chrome --executable-path | xargs dirname):$PATH" && node server.js
    envVars:
      - key: PORT
        value: 3000
